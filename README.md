## Project using React Native Reanimated

#### Links

I used Expo + React Native Reanimated => [Link](https://docs.expo.dev/versions/latest/sdk/reanimated/).

### Running the project

Use `yarn` to install dependencies and then `yarn start` to run.

##### by thiagonovato
